# Support Triage & KB Answering

Take-home practical: route classification (Part A), KB question answering with
temporal correctness (Part B), and screenshot routing (Part C, light).

Work in progress — sections are filled in as each part lands. See commit
history for the build order.

## Setup

```bash
uv sync          # or: pip install -e . && pip install pytest
uv run pytest    # run tests
```
